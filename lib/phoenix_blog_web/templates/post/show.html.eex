<h1><%= @post.title %></h1>
<strong><%= readable_date(@post.published_at)%></strong>
<div>
  <%= @post.content %>
</div>

<hr />
<%= link "Back to all posts", to: Routes.post_path(@conn, :index), class: "button" %>